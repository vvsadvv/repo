import './index.css';
import GsrasApp from './GsrasApp';

/* Делает: Рендерит React-компонент GsrasSiteEntry и связывает его с состоянием и обработчиками модуля. Применение: экспортируется из модуля и используется UI-кодом проекта. */
export default function GsrasSiteEntry() {
  return <GsrasApp />;
}
